export interface PromoModalData {
  color: string;
  backgroundColor: string;
  title: string;
  text: string;
  ctaUrl: string;
  ctaText: string;
}

export type Category = {
  id: string;
  label: string;
  icon: string;
};
