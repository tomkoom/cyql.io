type ProjectData = record { title : text };
type ProjectProposal = record {
  id : text;
  projectData : ProjectData;
  proposalNumber : nat64;
  proposer : principal;
};
service : () -> {
  addProjectProposal : (ProjectData) -> (opt ProjectProposal);
  getProjectProposals : () -> (vec ProjectProposal) query;
}