import React, { FC } from "react"
import styled from "styled-components"

const Nfts: FC = (): JSX.Element => {
  return <NftsStyled>Nfts</NftsStyled>
}

const NftsStyled = styled.div``

export default Nfts
